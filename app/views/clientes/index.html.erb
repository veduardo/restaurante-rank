<h1>Clientes que participaram:</h1>

<table>
  <tr>
    <th>Nome</th>
    <th>Idade</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @clientes.each do |cliente| %>
  <tr>
    <td><%= cliente.nome %></td>
    <td><%= cliente.idade %></td>
    <td><%= link_to 'Show', cliente %></td>
    <td><%= link_to 'Edit', edit_cliente_path(cliente) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Cadastrar novo cliente', new_cliente_path %>
